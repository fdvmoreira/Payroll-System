/**
 * Enumerate the type of value entered by the user (used to get the user inputs)
 */
package com.fdvmlab;

public enum InputType {
    BYTE,
    CHARACTER,
    SHORT,
    INTEGER,
    FLOAT,
    LONG,
    DOUBLE,
    STRING
}
